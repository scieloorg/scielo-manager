[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "scielo-manager"
version = "0.0.0"
requires-python = ">=3.13"
description = "SciELO Manager"
